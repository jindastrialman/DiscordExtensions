<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Discord.Net" Version="3.5.0" />
    <Compile Include="../IKeys.cs" />  <!-- this interface for keyHolder, in case you want use Keys.cs -->
    <Compile Include="../Keys.cs" />  <!-- this class for keys, should never used not in any Program.cs -->
  </ItemGroup>

</Project>
